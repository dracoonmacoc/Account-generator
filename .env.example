# Railway Environment Variables Configuration
# Copy these to Railway's environment variables section

# Number of accounts to create
ACCOUNTS_TO_CREATE=5

# File paths (relative to /app in Docker)
ACCOUNTS_FILE=accounts.txt
COOKIES_FILE=cookies.txt

# Browser settings (MUST be true for Railway)
HEADLESS=true

# Proxy settings (optional)
USE_PROXY=false
PROXY=

# Nopecha CAPTCHA solver (recommended for Railway)
USE_NOPECHA=true
NOPECHA_KEY=your_nopecha_key_here

# Password settings
RANDOM_PASSWORD=true
FIXED_PASSWORD=TestPass123!

# Username settings
USE_USERNAME_BASE=

# Timing settings (in minutes)
CAPTCHA_TIMEOUT_MINUTES=5
REQUEST_LIMIT_WAIT_MINUTES=30

# Cookie consent (set to true if in EU)
HAS_COOKIES_PROMPT=false

# Railway specific (auto-detected)
RAILWAY_ENVIRONMENT=production
```

## 📁 File 4: `.dockerignore` (in root directory)
```
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/
.env
.venv
venv/
ENV/
.git/
.github/
.gitignore
*.md
!README.md
.DS_Store
.vscode/
.idea/
